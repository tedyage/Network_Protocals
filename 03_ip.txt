IP （Internet Protocal）因特网协议

IP 工作在网络层，面向计算机的，用于将传输层的数据段打包传输到网络当中的协议。

IP协议的工作原理是通过IP寻址来确定数据传输的目的地，每一台计算机都有一个唯一的IP地址，
将IP包传输到IP路由器，由IP路由器负责将这个包路由值目的地计算机或者下一台路由器，最终到达目的地计算机。

IP协议特点：
1.无连接。
2.不可靠，发送方无法保证发送出去的数据都能正确的到达目的地，
  接收方，也不会校验所接收到的数据是否被损坏。
  无序，不会按照发送顺序传输到目的地。